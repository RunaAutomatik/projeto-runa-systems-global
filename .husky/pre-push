# Run lint/typecheck only in apps that have these scripts
for app in apps/*/; do
  if [ -f "$app/package.json" ]; then
    if cd "$app" && npm run lint --if-present && npm run typecheck --if-present; then
      cd - > /dev/null
    else
      cd - > /dev/null
      exit 1
    fi
  fi
done
