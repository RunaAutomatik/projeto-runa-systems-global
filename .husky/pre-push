# Monorepo pre-push: run typecheck only (lint requires ESLint setup per-app)
for app in apps/*/; do
  if [ -f "$app/package.json" ]; then
    cd "$app"
    npm run typecheck --if-present
    cd - > /dev/null
  fi
done
